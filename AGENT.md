# AGENT.md

## 개발 순서 원칙 (TDD)

반드시 아래 순서로 개발해라. 순서를 바꾸지 말 것.

1. 테스트 먼저 작성 (실패하는 테스트)
2. pytest로 테스트가 실제로 실패하는지 확인
3. 테스트를 통과하는 최소한의 코드 작성
4. make lint 통과 확인 (실패하면 통과할 때까지 스스로 수정)
5. 필요하면 리팩토링, 리팩토링 후 테스트 다시 통과 확인
6. 모두 통과한 후에만 사람에게 리뷰 요청

```
pytest
```

```
make lint
```

절대 하지 말 것:
- 테스트 없이 프로덕션 코드 먼저 작성
- 테스트를 나중에 추가하는 것
- 통과시키려고 테스트를 수정하는 것 (코드를 수정해야 함)

## 설계 원칙

- 함수는 한 가지 일만 한다
- 파일당 최대 200줄
- 함수당 최대 30줄
- 순환 import 금지
- 매직 넘버 금지 (상수로 분리할 것)
- 중복 코드 금지 (동일 로직은 함수로 추출)

## 테스트 작성 원칙

- 테스트 코드량이 프로덕션 코드량의 150%를 넘지 않도록
- 같은 경로를 반복 검사하는 테스트 금지
- 커버리지 채우기용 의미없는 테스트 금지

## 프로젝트 컨텍스트

- DART OpenAPI를 폴링해서 키워드 매칭 공시를 출력하는 스크립트
- 단일 실행 스크립트 (폴링 루프 없음, 인프라단에서 반복 호출)
- 상태는 state.json으로 관리
- 결과는 results/YYYY-MM-DD.md에 키워드별로 저장
- 로그는 app.log에 기록
